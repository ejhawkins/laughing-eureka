Place your Boldonse font files here so Vite can resolve them at build time.

Expected files:
- Boldonse.woff2
- Boldonse.woff (optional fallback)

After adding the files, commit them (or add them via your CI/CD or hosting provider) so the build can reference /fonts/Boldonse.woff2 and /fonts/Boldonse.woff successfully.

